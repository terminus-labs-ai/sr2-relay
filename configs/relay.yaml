llm:
  model: "gpt-4o"
  api_key: "${OPENAI_API_KEY}"
  # base_url: "http://localhost:11434/v1"  # Uncomment for local models
  # fast_model: "gpt-4o-mini"             # Defaults to model if unset
  # embed_model: "text-embedding-3-small"  # Defaults to model if unset

server:
  host: "0.0.0.0"
  port: 8080

sr2_config_dir: "${SR2_CONFIG_DIR}"
sr2_agent_yaml: "agent.yaml"
